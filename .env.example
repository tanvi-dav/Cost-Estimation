# Copy this file to ".env" and paste your Gemini key after the = sign.
# Get a free key at https://aistudio.google.com/apikey
# Do NOT commit your real .env — .gitignore already excludes it.
GEMINI_API_KEY=

# Optional: override the model if your key lacks access to the default.
# GEMINI_MODEL=gemini-2.0-flash